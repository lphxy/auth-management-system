<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wan.cms.service.mapper.UserVOMapper">

    <!-- 缓存 -->
    <!--<cache type="PERPETUAL" eviction="LRU" flushInterval="60000" size="1024" readOnly="true"/>-->

    <!-- 结果映射关系 -->
    <resultMap id="UserResultMap" type="userVO">
        <id column="user_id" property="userId"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="nickname" property="nickname"/>
        <result column="sex" property="sex"/>
        <result column="ctime" property="ctime"/>
        <result column="content" property="content" />
        <collection column="user_id" property="books" ofType="Book">
            <id column="book_id" property="bookId"/>
            <result column="user_id" property="userId"/>
            <result column="name" property="name"/>
        </collection>
    </resultMap>

    <!-- 查询一对多的所有记录 -->
    <select id="selectUserWithBook" resultMap="UserResultMap" parameterType="int">
        select
        u.user_id userId,u.username,u.password,u.nickname,u.sex,u.ctime,u.content,
        b.book_id bookId,b.user_id userId,b.name
        from
        user u
        left join
        book b
        on
        u.user_id=b.user_id
        where
        u.user_id=#{id,jdbcType=INTEGER}
    </select>

    <cache type="org.mybatis.caches.ehcache.LoggingEhcache" />

</mapper>